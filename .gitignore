node_modules/
.DS_Store
*.log
.env

# raw forge source screenshots — local-only, not pushed (large binaries; content is
# extracted into forge/*.md instead)
forge/*.PNG
forge/*.png
forge/*.JPG
forge/*.jpg
forge/*.JPEG
forge/*.jpeg
forge/*.MOV
forge/*.mov
