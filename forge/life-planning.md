# Forge: life-planning (proposed)

Staging only — not yet reviewed into the live tree. **No existing taxonomy match** — flagged as a new category for later review.

---

_"6-Step Life Planning Framework" (source_id 2025-04-30-c) — the raw unattributed artifact was discarded (same treatment as `life-audit.md`'s source graphic), but the underlying mechanism was placed, unified with `map-life-domain-audit` into one circular cycle, as `map-life-direction-cycle` (`layers/04-map/map-life-direction-cycle.md`) on 2026-07-19 — see `backlog/_processing_log.md` for provenance. File kept, not deleted, per this repo's backlog convention._

